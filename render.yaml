services:
  - type: web
    name: heathrow-caveman-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: AVIATIONSTACK_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.4
